Cách gỡ theo dõi 1 file:
git rm --cached <tên file>
git commit -m '...gỡ theo dõi file <tên file>' commit nó lại

application.dev.yaml:
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/swp391?useSSL=false&allowPublicKeyRetrieval=true
    username: ...
    password: ...

  config:
    import: "optional:file:.env[.properties]"
